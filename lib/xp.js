module.exports = {
	load: () => new Promise((resolve, reject) => {
		// TODO: Load all user XP into memory.
	}),

	set: (user, xp) => new Promise((resolve, reject) => {
		// TODO: Set a user's XP in memory and on SQL server.
	})
};
